
<div class="grid min-h-screen place-items-center  mt-6">
  <div class="w-gul p-12 form_wrapper sm:w-8/12 md:w-1/2 lg:w-3/12  shadow-2xl mb-12 bg-primary-200 rounded-lg">
    <h1 class="text-4xl	 font-bold text-center"> <span class="umbo-header text-lb:text-6xl ">Connexion</span></h1>    

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="field mt-10">
          <%= f.email_field :email, autofocus: true, placeholder: 'Email', autocomplete: "email", class:"block w-full p-3 mt-2   bg-gray-100  border-2 border-gray-100 focus:border-primary-600 rounded-xl placeholder-gray-500" %>
          <br />
        </div>

    <div class="field">
          <%= f.password_field :password, placeholder: 'Mot de passe', autocomplete: "new-password", class:"block w-full p-3 mt-2   bg-gray-100  border-2 border-gray-100 focus:border-primary-600 rounded-xl placeholder-gray-500	" %>
          <% if @minimum_password_length %>
          <span class="text-xs italic text-gray-500 mt-1 ml-2">(<%= @minimum_password_length %> caractères minimum)</span>
          <% end %><br />
        </div>

    <% if devise_mapping.rememberable? %>
      <div class="field mt-2 ml-1">
        <%= f.check_box :remember_me, class:"mt-1" %>
        <%= f.label :remember_me, class:"text-sm font-semibold text-gray-600 ml-1 mb-1" %>
      </div>
    <% end %>

    <div class="actions flex justify-center   ">
          <%= f.submit "Valider", class:" block w-full mt-5 bg-primary-500 hover:bg-primary-400 py-4 px-6 block rounded-xl text-white cursor-pointer font-black" %>
        </div>

    <% end %>

    <div class="mt-6 text-sm ml-1">
      <%= render "devise/shared/links"%>
    </div>

  
  </div>
</div>
